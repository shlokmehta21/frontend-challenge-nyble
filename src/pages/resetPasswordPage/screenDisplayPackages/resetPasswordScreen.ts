import { ScreenDisplayPackage } from 'pages/screenDisplayPackageType';

const ResetPasswordScreenDisplayPackage: ScreenDisplayPackage<any> = {
  route: '/allScreens/resetPasswordPage_resetPassword',
  pageState: {},
};

const resetPasswordScreenScreenDisplayPackages = [ResetPasswordScreenDisplayPackage];

export default resetPasswordScreenScreenDisplayPackages;
